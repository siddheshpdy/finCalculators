# Gemini Code Assist Instructions

1. **CSS Guidelines**: Always add CSS to CSS files (e.g., `*.module.css`) and avoid using inline styles in JSX files.
2. **Service Layer**: Add API calls and business logic calculations to service files or hooks (e.g., `useFinance.js`) instead of implementing them directly in components.